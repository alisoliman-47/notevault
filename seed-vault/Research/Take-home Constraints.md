# Take-home Constraints

Per the brief:

- No graph view, tags, or themes in v1.
- MCP is required; UI must share the vault layer with MCP tools.
- Optional: pick at most one extra (semantic search, graph, agent log, read-only MCP).

This seed vault exists so reviewers can run search and backlinks without authoring data first.

Links: [[Start Here]], [[Archive/Old Idea]].
